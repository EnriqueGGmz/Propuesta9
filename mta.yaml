_schema-version: "3.2"
ID: store
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: store-destination-content
  type: com.sap.application.content
  requires:
  - name: store-destination-service
    parameters:
      content-target: true
  - name: store-repo-host
    parameters:
      service-key:
        name: store-repo-host-key
  - name: store-uaa
    parameters:
      service-key:
        name: store-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: store_html_repo_host
          ServiceInstanceName: store-html5-srv
          ServiceKeyName: store-repo-host-key
          sap.cloud.service: store
        - Authentication: OAuth2UserTokenExchange
          Name: store_uaa
          ServiceInstanceName: store-xsuaa-srv
          ServiceKeyName: store-uaa-key
          sap.cloud.service: store
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: store-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: store-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - store.zip
      name: store
      target-path: resources/
- name: store
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: store-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: store-destination-service
    service-plan: lite
- name: store-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: store-xsuaa-srv
    service-plan: application
- name: store-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: store-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
